{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" }
  ]
}
```

This is the standard Vite pattern — the root `tsconfig.json` just delegates to `tsconfig.app.json`, which already has `"include": ["src"]` and will pick up your `src/types/gsap-trial.d.ts`.

---

## Fix 4 — Verify your `src/types/` folder path

Make sure the file is at exactly:
```
src/
  types/
    gsap-trial.d.ts   ← must be here
  components/
    utils/
      initialFX.ts
      splitText.ts{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" }
  ]
}
```

This is the standard Vite pattern — the root `tsconfig.json` just delegates to `tsconfig.app.json`, which already has `"include": ["src"]` and will pick up your `src/types/gsap-trial.d.ts`.

---

## Fix 4 — Verify your `src/types/` folder path

Make sure the file is at exactly:
```
src/
  types/
    gsap-trial.d.ts   ← must be here
  components/
    utils/
      initialFX.ts
      splitText.ts